// Jest setupFilesAfterEnv — runs after the test framework is installed.
// Source modules are committed under assets/js/life-money/ and do not need
// to be generated here.

// Keep this file as an explicit Jest setup entry so repository configuration
// and test bootstrap behavior stay aligned, even when no shared per-test
// initialization is currently required.
beforeEach(() => {});
